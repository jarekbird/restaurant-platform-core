Add menu lookup helpers for chat

- Implement helper functions (e.g., `findMenuItemByName(menu, name)`) for fuzzy/exact matching.
- Ensure helpers consider categories, item names, and synonyms where possible.

