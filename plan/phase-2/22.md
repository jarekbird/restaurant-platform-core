Define shared chat types

- Add a small types module (e.g., `lib/ai/types.ts`) defining `ChatMessage`, `ChatRole`, and `ChatAction`.
- Ensure types align with how `chatService` and `actionParser` will be implemented.

