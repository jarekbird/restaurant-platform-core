Implement cart persistence (optional)

- Persist cart state to `localStorage` (or similar) in `useCart`.
- Restore persisted cart on app load.
- Ensure hydration logic avoids mismatches between server and client.

