Create `CartProvider` context shell

- Add `components/order/CartProvider.tsx` with a React context typed to the `useCart` return value.
- Export `CartProvider` and `useCartContext` (throwing if used outside provider).

