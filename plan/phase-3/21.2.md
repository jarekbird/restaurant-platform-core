# TASK-021.2: Implement OrderingConfig Model

**Section**: 6. Direct Ordering, Loyalty, Offers, and Repeat Orders
**Subsection**: 6.1.2
**Task ID**: TASK-021.2
**Parent Task**: TASK-021

## Description

Implement the OrderingConfig model that defines how restaurants handle direct ordering and third-party marketplace links.

## Current State

- Ordering models module exists (from TASK-021.1)
- No OrderingConfig exists

**Prerequisites**: TASK-021.1 must be completed

## Checklist

### Implementation Steps

- [ ] Step 1: Implement OrderingConfig model
  - [ ] Define `OrderingConfig` class with:
    - [ ] `direct_ordering_enabled: bool` (required, default True)
    - [ ] `direct_ordering_url: str` (required if enabled)
    - [ ] `marketplace_links: Dict[str, str]` (optional)
    - [ ] `show_marketplace_links: bool` (default False)
    - [ ] `marketplace_link_placement: str` (optional)
    - [ ] `pickup_enabled: bool` (default True)
    - [ ] `delivery_enabled: bool` (default False)
    - [ ] `reservations_enabled: bool` (default False)
    - [ ] `reservations_url: Optional[str]`
- [ ] Step 2: Add validation
  - [ ] direct_ordering_url required if direct_ordering_enabled
  - [ ] Validate URLs are valid
- [ ] Step 3: Add helper methods
  - [ ] `has_marketplace_links() -> bool`

### Testing

- [ ] Write unit tests: `tests/test_models_ordering_config.py`
  - [ ] Test OrderingConfig creation
  - [ ] Test validation for required fields
  - [ ] Test configs for restaurants with only direct ordering
  - [ ] Test configs for restaurants with direct + marketplace links

## Related Tasks

- Parent: TASK-021
- Previous: TASK-021.1 (Module structure)
- Next: TASK-021.3 (LoyaltyConfig model)

## Definition of Done

### Task Type: CODE/FILE WRITING TASK

**Automated Tests**:
- [ ] Unit tests exist and pass
- [ ] Test coverage >= 85%

**Git & Version Control**:
- [ ] Changes committed: `git commit -m "TASK-021.2: Implement OrderingConfig model"`
- [ ] Code pushed to origin

