# TASK-1.3.1: Extend Theme interface with accent and optional tokens

**Section**: 1. Theme System & Owner-Style Theme Variants
**Subsection**: 1.3.1
**Task ID**: TASK-1.3.1

## Description

Extend the `Theme` interface in `lib/themes/index.ts` to add the `accent` color token and optional shape tokens (`radii.card`, `radii.button`, `radii.badge`). This is the first part of extending the Theme type - only the interface definition, not updating existing themes.

**Reference Implementation**: 
- `lib/themes/index.ts` (current Theme interface)

## Current State

The current `Theme` interface has basic color tokens but is missing `accent` color and optional shape tokens.

**Important**: This task should be executed after TASK-0.2, and before TASK-1.3.2.

## Checklist

### Implementation Steps

- [ ] Step 1: Add `accent` color token
  - [ ] Add `accent: string` to `colors` object in Theme interface
- [ ] Step 2: Add optional shape tokens
  - [ ] Add optional `radii` object to Theme interface
  - [ ] Include `card`, `button`, `badge` properties
  - [ ] Make `radii` optional (using `?` or `Partial<>`)

### Testing

- [ ] Write unit tests:
  - [ ] Test that Theme interface includes `accent` in colors
  - [ ] Test that Theme interface includes optional `radii` object
- [ ] Run type checking: `npm run type-check`

## Related Tasks

- Previous: TASK-0.2
- Next: TASK-1.3.2
- Dependencies: TASK-0.2

## Definition of Done

This is a **CODE/FILE WRITING TASK**.

**Definition of Done**: "Theme interface has been extended with `accent` color token and optional `radii` shape tokens. Type checking passes. Code is committed and pushed to origin."

**User Stories**:
- As a developer, I want an `accent` color token so I can add secondary emphasis elements
- As a developer, I want optional shape tokens so I can control border radii consistently

**Automated Tests**: 
- Type checking: `npm run type-check` (verifies interface is valid)

**Git Commit & Push**: 
- Commit with message: `feat(phase-3): extend Theme interface with accent and shape tokens (TASK-1.3.1)`
- Push to origin: `git push origin <branch-name>`

