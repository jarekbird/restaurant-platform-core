# TASK-012.2: Implement Base Restaurant Schema Builder

**Section**: 4. Structured Data (Schema.org)
**Subsection**: 4.1.2
**Task ID**: TASK-012.2
**Parent Task**: TASK-012

## Description

Implement the main `build_restaurant_schema()` function that creates the base Restaurant JSON-LD schema with all required and optional fields. This includes name, image, address, geo, cuisine, telephone, URL, hours, price range, ratings, and menu link.

## Current State

- Schema generator module exists (from TASK-012.1)
- No schema builder exists

**Prerequisites**: TASK-012.1 must be completed

## Checklist

### Implementation Steps

- [ ] Step 1: Implement base schema builder
  - [ ] Add `build_restaurant_schema(restaurant: Restaurant, location: Optional[Location] = None) -> dict` function
  - [ ] Create base schema structure:
    - [ ] `@context: "https://schema.org"`
    - [ ] `@type: "Restaurant"`
  - [ ] Add required fields:
    - [ ] `name`, `image`, `address`, `geo`, `servesCuisine`, `telephone`, `url`, `openingHoursSpecification`, `priceRange`
  - [ ] Add optional fields:
    - [ ] `aggregateRating` (if rating exists)
    - [ ] `menu` (if menu exists)
- [ ] Step 2: Use helper functions (to be created in later subtasks)
  - [ ] Call address formatting helper
  - [ ] Call hours conversion helper
  - [ ] Call geo formatting helper

### Testing

- [ ] Write unit tests: `tests/test_generators_schema_restaurant.py`
  - [ ] Test schema creation with full restaurant data
  - [ ] Test presence of all required keys
  - [ ] Test @type is "Restaurant"

## Related Tasks

- Parent: TASK-012
- Previous: TASK-012.1 (Module structure)
- Next: TASK-012.3 (Hours conversion helper)

## Definition of Done

### Task Type: CODE/FILE WRITING TASK

**Automated Tests**:
- [ ] Unit tests exist and pass
- [ ] Test coverage >= 80%

**Git & Version Control**:
- [ ] Changes committed: `git commit -m "TASK-012.2: Implement base restaurant schema builder"`
- [ ] Code pushed to origin

