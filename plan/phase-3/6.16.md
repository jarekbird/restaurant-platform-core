# TASK-6.16: Implement SEO metadata helpers

**Section**: 6. SEO & AI Search Structure in the Prototype
**Subsection**: 6.16
**Task ID**: TASK-6.16

## Description

Add a helper in `lib/seo/restaurantMetadata.ts` that implements `buildRestaurantMetadata(config: RestaurantConfig, pageType: 'home' | 'menu' | 'catering' | 'index')` using title/description templates from the master plan (name + cuisine + city + "Order Online", etc.).

**Reference Implementation**: 
- `master-plan-redacted.md` Section 5.1 (Metadata Helpers)
- `lib/schemas/restaurant.ts` (RestaurantConfig schema)

## Current State

No SEO metadata helpers exist. Preview pages don't set explicit SEO metadata.

**Important**: This task should be executed before TASK-6.17.

## Checklist

### Implementation Steps

- [ ] Step 1: Create metadata helper file
  - [ ] Create `lib/seo/restaurantMetadata.ts`
- [ ] Step 2: Implement buildRestaurantMetadata function
  - [ ] Accept `config: RestaurantConfig` and `pageType` parameters
  - [ ] Generate title: "[Name] - [Cuisine] in [City] | Order Online"
  - [ ] Generate description: "[Name] offers authentic [cuisine] in [city]. Order online for delivery or pickup."
  - [ ] Return metadata object compatible with Next.js Metadata API
- [ ] Step 3: Support different page types
  - [ ] 'home': Main restaurant page
  - [ ] 'menu': Menu page (if separate)
  - [ ] 'catering': Catering page (if separate)
  - [ ] 'index': Index/listing page

### Testing

- [ ] Write unit tests:
  - [ ] Test buildRestaurantMetadata with sample configs
  - [ ] Test different page types
  - [ ] Test title and description formats
- [ ] Run full test suite: `npm test`

## Related Tasks

- Previous: TASK-5.15
- Next: TASK-6.17
- Dependencies: None

## Definition of Done

This is a **CODE/FILE WRITING TASK**.

**Definition of Done**: "SEO metadata helper has been created in `lib/seo/restaurantMetadata.ts` with `buildRestaurantMetadata` function. Unit tests verify metadata generation for different page types. All tests pass. Code is committed and pushed to origin."

**User Stories**:
- As a search engine, I want proper page titles and descriptions so I can index restaurant pages correctly
- As a developer, I want a reusable metadata helper so I can generate SEO metadata consistently

**Automated Tests**: 
- Unit tests for buildRestaurantMetadata with sample configs
- Unit tests for different page types
- Unit tests for title and description formats

**Git Commit & Push**: 
- Commit with message: `feat(phase-3): implement SEO metadata helpers (TASK-6.16)`
- Push to origin: `git push origin <branch-name>`
